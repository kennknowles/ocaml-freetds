(jbuild_version 1)

(executables
 ((names (test_dblib))
  (libraries (freetds oUnit))))

(alias
 ((name test)
  (deps (test_dblib.exe))))

(alias
 ((name runtest)
  (deps (test_dblib.exe))
  (action (run ${<}))))
